version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8034:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - kafe-network

networks:
  kafe-network:
    driver: bridge
